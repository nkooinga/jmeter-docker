version: '2'

services:

  master:
    image: nkooinga/jmeter-docker/jmmaster
    container_name: master
    tty: true
    hostname: master
    networks:
      - poct
  slave:
    image: nkooinga/jmeter-docker/jmserver
    tty: true
    networks:
      - poct

networks:
  poct:
    driver: bridge
